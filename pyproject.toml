[tool.ruff]
line-length = 110
target-version = "py313"
exclude = ["migrations", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]

[tool.black]
line-length = 110
target-version = ["py313"]
extend-exclude = "migrations"
